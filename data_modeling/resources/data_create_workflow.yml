resources:
  jobs:
    data_create_workflow:
      name: "Data Creation Workflow"
      
      email_notifications:
        on_failure:
          - vikassingh0593@gmail.com
        on_success:
          - vikassingh0593@gmail.com
      
      tasks:
        - task_key: create
          description: "Load raw data from source systems"
          notebook_task:
            notebook_path: ../src/Data_Create.ipynb
            base_parameters:
              environment: "dev"
          timeout_seconds: 3600
          max_retries: 1
      
      max_concurrent_runs: 1
      timeout_seconds: 14400
